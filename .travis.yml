sudo: required
services:
  - docker

before_install:
  - docker build -t geoteo/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true geoteo/docker-react npm run test -- --watchAll=false